[project]
name = "ai-coding-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Shresth", email = "shrxsth16@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "google-genai==1.12.1", #SDK for gemini API which we will be using
    "python-dotenv==1.1.0",
]

[project.scripts]
ai-coding-agent = "ai_coding_agent:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
